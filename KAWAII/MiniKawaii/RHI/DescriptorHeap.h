#pragma once

namespace RHI
{
	/*
	* Descriptor are structure which tell shader where to find the resource, and how interprete data resource.
	* Descriptor heap are chunk of memory where descriptro are stored. Shader Visible(CBV, UAV, Sampler, SRV), Shader No Visible(RTV, DSV, IBV, VBV, and SOV)
	* 
	*/

}